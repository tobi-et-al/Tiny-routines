# Tiny Routines on GitHub Pages

Upload these files to the publishing branch or directory configured for GitHub Pages.

- The site is static and starts from `index.html`.
- Shared records use the Supabase URL and publishable key already configured in `app.js`.
- Run `supabase.sql` once in the Supabase SQL editor if the shared table is not already set up.
- The access question is: `19 / 09 / 2026`.
